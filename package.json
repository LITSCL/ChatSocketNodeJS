{
  "name": "ChatSocketNodeJS",
  "version": "1.0.0",
  "description": "Chat en tiempo real con sockets y NodeJS",
  "main": "servidor/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Daniel",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^4.0.0"
  }
}
